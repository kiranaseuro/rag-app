# Sample project

This folder contains project-specific tfvars and backend config files.

- Use `dev.tfvars`, `qa.tfvars`, and `prod.tfvars` to set project_name, use_case, and credentials.
- Update backend files to point at your remote Terraform state bucket and lock table.
